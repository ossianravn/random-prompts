interface:
  display_name: "Agent Skill Engineer"
  short_description: "Build and evaluate reusable Agent Skills"
  default_prompt: "Turn this workflow, prompt, or existing bundle into the smallest reusable, validated Agent Skill."
policy:
  allow_implicit_invocation: false
